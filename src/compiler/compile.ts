export function compile(source: string): string {
  // TODO. Compile the source code to 6510 assembly source code.
  const assemblySourceCode = compileToAssembly(source);
  return assemblySourceCode;
}

function compileToAssembly(source: string): string {
  // TODO. Compile the source code to 6510 assembly source code.
  return source;
}
